export interface ICreateSchedulePayload {
    startDate: Date;
    endDate: Date;
    startTime: string;
    endTime: string;
}

export interface IUpdateSchedulePayload {
    startDate: Date;
    endDate: Date;
    startTime: string;
    endTime: string;
}